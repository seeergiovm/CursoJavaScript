//El operador ternario

const autenticado = false;
const puedePagar = true;

console.log(autenticado && puedePagar ? 'Esta autenticado' : 'No lo está')

//existen ternarios anidados pero poco común, un poco lioso