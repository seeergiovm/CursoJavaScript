//Recorrer un array
const meses = ['Enero', 'Febrero', 'Marzo', 'Abril', 'Mayo'];

console.table(meses[1]);

// Cuanrto mide el arreglo
console.log(meses.length);

for (let i = 0; i < meses.length; i++) {
    console.log(meses[i])
}